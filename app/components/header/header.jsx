import React from 'react';

import './header.less';

const Header = React.createClass({
  render() {
    return (
      <header role="banner">
        <h1>Annotator</h1>
      </header>
    );
  }
});

export default Header;
