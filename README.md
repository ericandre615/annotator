# Annotator

### Install
`git clone https://github.com/ericandre615/annotator.git`

`cd annotator`

`npm install`

### Build
`npm run build`

### Start Server
`npm start`

### Build and run

shortcut to build and start Server

`npm run annotator`
